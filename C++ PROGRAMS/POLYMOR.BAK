#include<iostream.h>
#include<conio.h>
class overload
{
public:
int max(int, int);
float max(float, float);
};
int overload::max(int n1, int n2)
{
if(n1>n2)
return n1;
else
return n2;
}
float overload::max(float n1, float n2)
{
if(n1>n2)
return n1;
else
return n2;
}
int main()
{
overload a;
clrscr();
cout<<a.max(5.4F,5.9F)<<endl;
cout<<a.max(19,12)<<endl;
getch();
return 0;
}